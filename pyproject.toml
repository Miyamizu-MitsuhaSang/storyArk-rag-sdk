[project]
name = "translate-manager-rag"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pybind11>=2.13.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
]

[build-system]
requires = ["setuptools>=75.0", "wheel", "pybind11>=2.13.0"]
build-backend = "setuptools.build_meta"
